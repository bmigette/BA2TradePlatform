# my tools
nicegui>=3.0.0  # Requires NiceGUI 3.0+ - see migration notes in docs/NICEGUI_3_MIGRATION.md
alpaca-py==0.43.2
tastytrade==12.0.2  # pin: 12.x is the OAuth-only ASYNC rewrite (Account.place_order is a coroutine whose dry_run defaults to True). Do not float this line.
pandas-market-calendars>=5.1.1  # pin: NOT just a tastytrade transitive. ba2_common.core.market_calendar uses it for the offline holiday/half-day NYSE fallback behind get_market_hours(); losing it makes the allocation wizard's submit gate report "unavailable" and block forever.
sqlmodel
python-dotenv
yahooquery
APScheduler
alembic
reportlab<5  # pin: reportlab 5.0 untested on live trade app (PDF export) — keep on 4.x
plotly
fmpsdk
ib_async

# TradingAgents Tools
typing-extensions
langchain-openai
langchain-experimental
langchain_core
langchain_anthropic
langchain-text-splitters
pandas<3  # pin: pandas 3.0 (CoW default, dtype changes) untested on live trade app — keep on 2.x
yfinance
praw
feedparser
stockstats
eodhd
langgraph
chromadb
setuptools
torch  # Required by sentence-transformers
sentence-transformers
backtrader
akshare
tushare
finnhub-python
parsel
requests
tqdm
pytz
redis
chainlit
rich
questionary
langchain-anthropic
langchain-google-genai  # Google Gemini models - LangChain integration
google-genai  # Google native SDK for Search grounding
langchain-xai  # xAI (Grok) models - LangChain integration
xai-sdk  # xAI native SDK for Live Search
langchain-deepseek  # DeepSeek models
langchain-aws  # AWS Bedrock models
# Moonshot uses langchain_community.chat_models.moonshot (included in langchain-community)
trafilatura  # Web content extraction for LLMs
google-generativeai

tzlocal

# --- Extracted BA2 packages (Phase 6 migration) ---
# Implementation of core interfaces/providers/clean-experts now lives in these
# three sibling packages; the in-tree ba2_trade_platform.* modules become thin
# re-export shims. Installed editable into the live venv during development
# (BA2TradeCommon/install.sh --editable); pinned here to the git/SSH form that
# install.sh uses for non-editable deploys.
ba2trade-common @ git+ssh://git@github.com/bmigette/BA2TradeCommon.git@dev
ba2trade-providers @ git+ssh://git@github.com/bmigette/BA2TradeProviders.git@dev
ba2trade-experts[ui] @ git+ssh://git@github.com/bmigette/BA2TradeExperts.git@dev

# Testing
pytest>=8.0
pytest-cov>=5.0
